# Features

> Hand-written by you. Every feature the app needs, one line each, in rough
> priority order. Say what the user can do, not how it is built. Mark anything
> out of the first release as `later`. `/plan` turns this into `build-plan.md`.

## Must have (first release)
1. 
2. 
3. 

## Later
- 

## Explicitly not building
- 
