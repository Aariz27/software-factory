# UI direction

> Hand-written by you: images, words, or both. Drop reference images into
> `blueprint/mockups/` and list them here with what to take from each.
> `/plan` reads this. `/prototype ui` renders a static mockup from it.

## Look and feel in words
Palette, type, density, mood, what it should not look like:

## Reference images
| File in `blueprint/mockups/` | What to take from it |
|---|---|
| | |

## Components and layout rules
- 
